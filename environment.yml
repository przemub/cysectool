name: visualiser-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8.8  # For Mac M1 compatibility
  - pip=20.3.3
  - numpy=1.19.5
  - Pillow=8.1.1
  - pip:
      - -r file:requirements.txt
